package display;

/**
 * A display configuration, used in the Display to determine the displays of each component of the game.
 */
public enum DisplayConfiguration {
    DEFAULT,
    INVENTORY_SELECT,
    TILE_SELECT
}
